node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Kept on disk for optional Actions-based deploy, but not pushed:
# the current gh token lacks the `workflow` scope. To enable it later, run
# `gh auth refresh -s workflow` then un-ignore and commit this folder.
.github/workflows/
